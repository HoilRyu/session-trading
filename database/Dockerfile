ARG TIMESCALE_IMAGE=timescale/timescaledb:2.25.2-pg17

FROM ${TIMESCALE_IMAGE}

LABEL org.opencontainers.image.title="session-trading database" \
      org.opencontainers.image.description="TimescaleDB image for the session-trading local development environment"
