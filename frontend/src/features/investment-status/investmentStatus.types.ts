export type RunningJobItem = {
  id: string
  label: string
  status: string
  progressText: string
}

export type StrategyItem = {
  id: string
  label: string
  summary: string
  description: string
}
